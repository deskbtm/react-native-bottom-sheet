# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Context 7

Always use Context7 when I need library/API documentation, code generation, setup or configuration steps without me having to explicitly ask.

# Project rules (`.cursor/rules/`)

| File                               | When                        | Contents                                                                                |
| ---------------------------------- | --------------------------- | --------------------------------------------------------------------------------------- |
| `git.mdc`                          | Always                      | Conventional Commits, commit hygiene, git workflow                                      |
| `production-code-modification.mdc` | Always                      | Minimal diffs, preserve behavior, scope control, architecture preservation              |
| `expo-project.mdc`                 | Always                      | Layout, naming, oxfmt/oxlint compliance, modules, docs, React Compiler, deprecated APIs |
| `react-native-performance.mdc`     | Editing `src/**/*.{ts,tsx}` | Lists, render cost, styling, threading                                                  |
| `testing.mdc`                      | Always                      | Test placement (`__tests__/`, `e2e/`), naming, tooling                                  |
